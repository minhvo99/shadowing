Bạn đang chạy không người trực (`claude -p`) trong một git worktree riêng của repo này, checkout ở `origin/dev`.

Làm yêu cầu bên dưới theo đúng skill `ship-request`. Đừng hỏi lại giữa chừng vì không có ai trả lời. Chỗ nào mơ hồ nhưng có mặc định hợp lý thì chọn mặc định đó và ghi vào PR. Chỉ khi mơ hồ thật sự mới dừng và trả `needs_clarification`.

## Yêu cầu

{{REQUEST}}
